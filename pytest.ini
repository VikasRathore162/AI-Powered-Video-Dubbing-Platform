[pytest]
testpaths = tests
pythonpath = . tests
markers =
    integration: uses the real AI models (RUN_FULL_PIPELINE=1 pytest -m integration)
addopts = -m "not integration" -q
filterwarnings =
    ignore::DeprecationWarning
